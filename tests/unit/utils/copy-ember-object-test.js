import copyEmberObject from 'dummy/utils/copy-ember-object';
import { module, test } from 'qunit';

module('Unit | Utility | copy-ember-object', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = copyEmberObject();
    assert.ok(result);
  });
});
